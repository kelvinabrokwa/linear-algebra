/**
 * calculate the rank of a matrix
 * @param {Matrix} matrix
 * @return {Number} rank of the matrix
 */
export default function(matrix) {
    if (matrix.rank) {
        return matrix.rank;
    }
    // to do
    return null;
}
